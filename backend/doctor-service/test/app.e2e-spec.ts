/**
 * E2E tests require MongoDB (e.g. docker compose up -d).
 * Run: npm run test:e2e
 */
describe.skip('Doctor search (e2e) — enable when MongoDB is running', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
