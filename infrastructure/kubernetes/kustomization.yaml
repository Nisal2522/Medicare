# kubectl apply -k k8s/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 01-configmap.yaml
  - 02-secret.yaml
  # - 03-mongodb.yaml  # Using MongoDB Atlas instead of in-cluster pod
  - 04-rabbitmq.yaml
  - notification-service.yaml
  - auth-service.yaml
  - doctor-service.yaml
  - appointment-service.yaml
  - payment-service.yaml
  - patient-service.yaml
  - telemedicine-service.yaml
  - ai-service.yaml
  # - frontend.yaml  # Frontend deployed to Vercel, not in-cluster
  - ingress.yaml
